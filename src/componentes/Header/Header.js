import "./Header.css"

export default function Header() {
    return (
        <header className="header">
          <img src="/img/header.png" alt="header" title="Header" />
        </header>
      );
}

